<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PuzzleHub">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PuzzleHub.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity" />
        <activity android:name=".GameHubActivity" />
        <activity android:name=".MemoryMatchActivity" />
        <activity android:name=".SlidingPuzzleActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".ScoreHistoryActivity" />
        <activity android:name=".HowToPlayActivity" />
        <activity android:name=".AboutActivity" />
    </application>

</manifest>
